# Minimal Launcher / Widget Spec (2026-02-19)

## 1) 현재 구현 상태

### 핵심 동작
- 기본 화면(미니멀 모드)
  - 온도 표시 (`weather.temperatureCelsius`)
  - 공기질 요약 (`weather.airQualitySummary`)
  - 오늘 할 일 (`dailyTodo`)
- 뉴스 화면(뉴스 모드)
  - 최대 3줄 뉴스 요약 표시
- 위젯 클릭 시
  - 미니멀 ↔ 뉴스 모드 토글
  - 뉴스 모드 진입 시 10초 후 자동 복귀

### 설정 화면
- 텍스트 톤: 라이트 / 다크
- 폰트 크기: 작게 / 보통 / 크게
- 지역 입력 (예: Seoul, Busan)
- 관심분야 입력 (쉼표 구분)
- 오늘 할 일 입력
- 저장 버튼: 설정 저장 + 위젯 즉시 새로고침

### 저장 구조
- DataStore Preferences 사용
- 기본값:
  - textTone: `light`
  - fontSize: `medium`
  - region: `Daejeon`
  - interests: `특이점이 온다 념글`
  - dailyTodo: `10 min walk`

---

## 2) 개선 포인트 (우선순위)

### P0 (바로)
1. `.gitignore` 유지 점검 (build 산출물 추적 방지)
2. README 작성
   - 앱 목적
   - 설치/실행 방법
   - 위젯 동작 요약
3. 설정값 유효성 검증
   - 지역 빈값 처리
   - 관심분야 빈값 처리

### P1 (다음)
1. 날씨/뉴스 실패 시 예외 UI
   - "데이터를 불러오지 못했어요" 등
2. 뉴스 모드 UX 개선
   - 수동 고정 모드 옵션(자동복귀 on/off)
3. 텍스트 가독성
   - 다크 배경 대비 더 명확한 색상 정책

### P2 (확장)
1. 클릭 액션 확장
   - 더블탭/길게누름 액션 분리
2. 위젯 레이아웃 변형
   - 작은 사이즈 전용 간략 모드
3. 관심사 기반 뉴스 카테고리 정교화

---

## 3) 다음 작업 제안 (짧은 사이클)

1. `README.md` 초안 작성
2. 설정 입력값 검증 추가
3. 뉴스 로딩 실패 fallback 텍스트 적용
4. 커밋 메시지 규칙 통일
   - 예: `feat: ...`, `fix: ...`, `docs: ...`

---

## 4) 확인 필요 항목 (Owner 결정)

1. 프로젝트 명칭 고정: `Minimal Launcher` vs `Minimal Widget`
2. 뉴스 자동복귀 10초 고정 유지 여부
3. 관심분야 기본값 교체 여부
4. 출시 목표 범위
   - 개인용 MVP
   - Play Store 배포 전제
